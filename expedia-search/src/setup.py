from setuptools import setup

setup(name='expedia-search',
      version='1.0',
      description='Bing search from a CSV file and search results related to Expedia are inserted into another CSV'
                  'file',
      packages=['core', 'test'],
      )
