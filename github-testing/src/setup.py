from setuptools import setup

setup(name='github-testing',
      version='1.0',
      description='A round of GitHub testing to cover profile basic settings, main features used by developers to '
                  'create repositories, the search functionality, and the star feature.',
      packages=['utils', 'pages', 'tests'],
      )
